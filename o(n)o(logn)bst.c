#include <stdio.h>

void search(int arr[], int n, int target) {
   // your code here
   
}

int main() {
    int arr[20];
    int n, k;
    scanf("%d",&n);
    scanf("%d",&k);
    for(int i=0;i<n;i++) scanf("%d",&arr[i]);
    search(arr, n, k);
    return 0;
}